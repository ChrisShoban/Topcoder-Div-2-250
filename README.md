Fork this repo
In Eclipse, 
  click Windows -> 
  Open Perspective -> 
  Other -> 
  GIT Repo ->
  Clone

After the repo is forked and cloned locally
  File ->
  Import ->
  Local ->
  Import as existing Project
